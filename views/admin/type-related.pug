.form-group
	select(name= name data-validate= options.validate)
		option(value="") ---
		for row in related
			if row.id == value
				option(value= row.id selected)= row.description
			else
				option(value= row.id)= row.description
	.input-ok-indicator.material-icons check_circle
	.validation-help
